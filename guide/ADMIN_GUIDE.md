# 게임 데이터 관리 가이드

이 문서는 게임 데이터 파일을 수정하는 방법을 설명합니다.

---

## 📁 파일 구조

```
weapon-game-main/
├── data/
│   ├── items.js          # 아이템 데이터 (특수, 재료, 방지권)
│   ├── weapons.js        # 무기 데이터 (레벨별 검)
│   ├── shop.js           # 상점 데이터
│   ├── equipment.js      # 장비 데이터
│   ├── forge.js          # 대장간 조합 데이터
│   ├── achievements.js   # 업적 데이터
│   └── titles.js         # 칭호 데이터
```

---

## 🎯 1. 아이템 추가/수정 (`data/items.js`)

### **특수 아이템 추가**
```javascript
ITEMS.special.새아이템ID = {
    id: '새아이템ID',
    name: '아이템 이름',
    description: '아이템 설명',
    effect: {
        type: 'effectType',  // successRate, autoClick 등
        value: 10            // 효과 값
    },
    rarity: 'rare'          // common, uncommon, rare, epic, legendary
};
```

### **재료 아이템 추가**
```javascript
ITEMS.materials.새재료ID = {
    id: '새재료ID',
    name: '재료 이름',
    description: '획득 조건 설명',
    dropRate: 5,            // 드롭 확률 (%)
    minLevel: 10,           // 최소 레벨
    rarity: 'uncommon'
};
```

### **방지권 추가**
```javascript
ITEMS.protections.새방지권ID = {
    id: '새방지권ID',
    name: '방지권 이름',
    description: '방지 범위 설명',
    maxLevel: 20,           // 최대 보호 레벨
    rarity: 'rare'
};
```

**희귀도 색상**
- `common`: 회색 (#95a5a6)
- `uncommon`: 녹색 (#27ae60)
- `rare`: 파란색 (#3498db)
- `epic`: 보라색 (#9b59b6)
- `legendary`: 주황색 (#e67e22)
- `mythical`: 빨간색 (#e74c3c)

---

## ⚔️ 2. 무기 추가/수정 (`data/weapons.js`)

### **새 무기 레벨 추가**
```javascript
{
    level: 31,                      // 무기 레벨
    name: "신규 무기 이름",
    upgradeCost: 200000000,         // 강화 비용
    sellPrice: 195000000,           // 판매 가격 (보통 강화비의 95%)
    successRate: 25,                // 성공률 (%)
    clickGold: 25000,               // 클릭당 획득 골드
    specialEffect: "특수 효과 설명",
    visual: {
        image: "assets/weapons/sword_31.png",  // 이미지 경로
        color: "custom_color",                 // 색상 키
        particle: "custom_particle",           // 파티클 효과
        background: null                       // 배경 효과
    }
}
```

**밸런스 가이드**
- 레벨이 오를수록 강화 비용 약 1.5~2배 증가
- 성공률은 점진적으로 감소 (최소 25%)
- 클릭 골드는 레벨에 비례하여 증가
- 판매가는 강화 비용의 90~97%

---

## 🏪 3. 상점 아이템 수정 (`data/shop.js`)

### **워프권 추가**
```javascript
{
    id: "warp_x200",
    name: "워프권 x200",
    effect: "clickMultiplier",
    value: 5000,                // 클릭 배율
    price: 80000000,            // 가격
    unlimited: true             // 무제한 구매 가능 여부
}
```

### **방지권 추가**
```javascript
{
    id: "super_protection_x1",
    name: "슈퍼 방지권 x1",
    maxLevel: 35,               // 최대 레벨
    price: 150000000,
    unlimited: true
}
```

### **특별 아이템 추가**
```javascript
{
    id: "special_item",
    name: "특별 아이템",
    effect: "effectType",       // 효과 타입
    value: 50,                  // 효과 값
    permanent: true,            // 영구 효과 여부
    price: 40000000,
    purchaseLimit: 1            // 구매 제한 (0 = 무제한)
}
```

---

## 🔨 4. 대장간 조합 추가 (`data/forge.js`)

### **새 조합 레시피 추가**
```javascript
{
    id: "new_recipe",
    name: "새로운 조합",
    materials: [                // 필요 재료
        {item: "재료ID", count: 10},
        {item: "재료ID2", count: 5}
    ],
    result: {                   // 결과물
        item: "결과아이템ID",
        count: 3
    },
    effect: "protection"        // 효과 타입
}
```

---

## 🎽 5. 장비 추가/수정 (`data/equipment.js`)

### **새 장비 추가**
```javascript
{
    id: "new_equipment",
    name: "새 장비 이름",
    slot: "ring",               // 슬롯: head, necklace, ring, bracelet, gloves, back, charm, tool, feet
    effect: "clickGoldMultiplier",  // 효과 타입
    value: 40,                  // 효과 값
    rarity: "epic",             // 희귀도
    obtainable: ["random_box", "achievement"]  // 획득 방법
}
```

### **랜덤 상자 가중치 수정**
```javascript
RANDOM_BOXES.normal.items.push({
    equipment: "new_equipment",
    weight: 5                   // 가중치 (높을수록 자주 나옴)
});
```

---

## 🏆 6. 업적 추가/수정 (`data/achievements.js`)

### **새 업적 추가**
```javascript
{
    id: "new_achievement",
    name: "업적 이름",
    condition: {
        type: "weapon_level",   // 조건 타입 (아래 참조)
        value: 35               // 목표 값
    },
    reward: {
        gold: 1000000000,       // 골드 보상
        item: "아이템ID",       // 아이템 보상
        count: 5,               // 아이템 개수
        equipment: "장비ID",    // 장비 보상
        title: "칭호ID"         // 칭호 보상
    },
    hidden: false               // 숨김 업적 여부
}
```

**조건 타입**
- `weapon_level`: 무기 레벨 달성
- `total_gold`: 누적 골드 획득
- `total_clicks`: 총 클릭 횟수
- `consecutive_success`: 연속 성공
- `consecutive_failures`: 연속 실패
- `critical_upgrades`: 크리티컬 강화 횟수
- `unique_materials`: 고유 재료 종류 수
- `total_upgrades`: 총 강화 시도 횟수
- `unique_equipment`: 고유 장비 종류 수
- `forge_recipes`: 대장간 조합 횟수
- `weapon_level_20_without_protection`: 무방지권 +20 달성
- `success_at_30_percent`: 30% 이하 성공률에서 성공

---

## 👑 7. 칭호 추가/수정 (`data/titles.js`)

### **새 칭호 추가**
```javascript
{
    id: "new_title",
    name: "새 칭호 이름",
    condition: {
        type: "weapon_level",   // 획득 조건 타입
        value: 35               // 목표 값
    },
    effect: {
        type: "successRate",    // 효과 타입 (아래 참조)
        value: 5                // 효과 값
    },
    rarity: "legendary"         // 희귀도
}
```

**칭호 효과 타입**
- `clickGoldMultiplier`: 클릭 골드 증가 (%)
- `successRate`: 성공률 증가 (%)
- `criticalChance`: 크리티컬 확률 증가 (%)
- `goldRefundOnFail`: 실패 시 골드 환불 (%)
- `equipmentEffect`: 장비 효과 증가 (%)
- `materialDropRate`: 재료 드롭률 증가 (%)
- `autoClickSpeed`: 자동 클릭 속도 증가 (%)
- `allStats`: 모든 능력치 증가 (%)

---

## ⚠️ 주의사항

### **1. ID 중복 금지**
- 모든 아이템, 무기, 장비, 업적, 칭호는 고유한 ID를 가져야 합니다
- ID는 영문 소문자와 언더스코어(_)만 사용하세요

### **2. 밸런스 고려**
- 새 아이템/무기 추가 시 기존 콘텐츠와의 균형을 고려하세요
- 너무 강력한 효과는 게임 재미를 떨어뜨립니다
- 가격은 효과에 비례하여 설정하세요

### **3. 파일 저장 후 테스트**
- 수정 후 반드시 브라우저에서 테스트하세요
- 콘솔(F12)에서 에러가 없는지 확인하세요
- 게임을 초기화하고 새로 시작해보세요

### **4. 백업**
- 중요한 수정 전에는 파일을 백업하세요
- Git을 사용하면 변경 이력을 관리할 수 있습니다

### **5. 문법 체크**
- JavaScript 문법을 지켜주세요
- 쉼표(,), 따옴표("), 중괄호({}) 누락 주의
- 마지막 항목 뒤에는 쉼표를 빼도 됩니다

---

## 🔧 빠른 수정 예제

### **예제 1: 레벨 31 무기 추가**
`data/weapons.js` 맨 아래에 추가:
```javascript
    {
        level: 31,
        name: "차원을 초월한 검",
        upgradeCost: 250000000,
        sellPrice: 240000000,
        successRate: 25,
        clickGold: 30000,
        specialEffect: "차원 붕괴",
        visual: {
            image: "assets/weapons/sword_31.png",
            color: "dimension",
            particle: "dimension",
            background: null
        }
    }
];
```

### **예제 2: 신규 특수 아이템 추가**
`data/items.js`의 `ITEMS.special`에 추가:
```javascript
        super_hammer: {
            id: 'super_hammer',
            name: '최강 망치',
            description: '강화 성공률을 대폭 증가시킵니다',
            effect: {
                type: 'successRate',
                value: 10
            },
            rarity: 'legendary'
        }
```

### **예제 3: 상점에 아이템 추가**
`data/shop.js`의 `specialItems` 배열에 추가:
```javascript
        {
            id: "super_hammer",
            name: "최강 망치",
            effect: "successRate",
            value: 10,
            permanent: true,
            price: 100000000,
            purchaseLimit: 1
        }
```

---

## 📊 추천 밸런스 값

### **무기 레벨별 가이드**
| 레벨 | 강화 비용 | 성공률 | 클릭 골드 |
|-----|---------|-------|----------|
| 0-10 | 300-20K | 100-80% | 1-50 |
| 11-20 | 30K-1.5M | 75-55% | 75-800 |
| 21-30 | 2.5M-150M | 50-30% | 1.2K-20K |
| 31+ | 250M+ | 25%+ | 30K+ |

### **아이템 가격 가이드**
- **방지권**: 1M ~ 100M (레벨에 따라)
- **워프권**: 1M ~ 80M (배율에 따라)
- **특수 아이템**: 15M ~ 100M (효과에 따라)
- **장비 상자**: 5M ~ 200M (등급에 따라)

---

## 🆘 문제 해결

### **게임이 로드되지 않을 때**
1. 브라우저 콘솔(F12)에서 에러 확인
2. JavaScript 문법 오류 확인 (쉼표, 따옴표 등)
3. 모든 데이터 파일이 index.html에 로드되었는지 확인

### **아이템이 표시되지 않을 때**
1. ID가 올바른지 확인
2. items.js에 정의되어 있는지 확인
3. 브라우저 캐시를 지우고 새로고침 (Ctrl + Shift + R)

### **밸런스가 이상할 때**
1. 게임 데이터 초기화 (설정 → 데이터 초기화)
2. 수정한 값이 너무 크거나 작지 않은지 확인
3. 백업 파일로 복원

---

## 📞 지원

문제가 발생하거나 추가 기능이 필요한 경우:
- 게임 데이터 백업을 먼저 하세요
- 에러 메시지와 수정한 내용을 기록하세요
- 개발자에게 문의하세요

---

**마지막 업데이트**: 2025-12-10
**버전**: v1.1
